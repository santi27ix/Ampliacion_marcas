"use strict";

// vamos a jugar con booleanos (tipo booleano)
// tiene solo 2 valores "true" o "false" 
var bvalido = false; //alert('es valido?'+ bvalido);

bvalido = true;
console.log('es valido?' + bvalido);
bvalido = 5 > 3;
console.log('es valido?' + bvalido);
bvalido = 5 < 3;
console.log('es valido?' + bvalido);